namespace Incentive.Core.Enums
{
    /// <summary>
    /// Frequency at which targets are measured
    /// </summary>
    public enum TargetFrequency
    {
        Monthly,
        Quarterly,
        Yearly,
        Custom
    }
}
